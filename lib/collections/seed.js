Meteor.startup({
	console.log(Fake.word());

	// if (Vocabulary.find().count() === 0) {
	// 	for (let i = 0; i < 300; i++) {
	// Vocabulary.insert({
	// 	name: Fake.word(),
	// 	description: Fake.sentence([32])
	// })
	// 	}
	// };
});
